# Dashboard Access Password (server-side only — NOT exposed to client)
# Used to authenticate into the admin dashboard at /dashboard
# Ganti password ini sebelum deploy ke production!
# Default: admin123 — untuk review/testing
DASHBOARD_ADMIN_PASS=admin123
